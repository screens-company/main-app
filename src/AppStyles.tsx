import styled from 'styled-components';

export const OuterStylesWrapper = styled.div`
  * {
    font-family: 'Montserrat', sans-serif;
    box-sizing: border-box;
  }
  
  body {
    margin: 0;
  }
`;

export const Wrapper = styled.div`
  display: flex;
  justify-content: center;
`;